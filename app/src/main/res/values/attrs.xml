<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>
    <declare-styleable name="DynamicTagView">
        <attr name="tagColor" format="color" />
        <attr name="containerColor" format="color" />
        <attr name="tagPaddingTop" format="reference|dimension" />
        <attr name="tagPaddingLeft" format="reference|dimension" />
        <attr name="tagPaddingRight" format="reference|dimension" />
        <attr name="tagPaddingBottom" format="reference|dimension" />
        <attr name="tagDTextSize" format="reference|dimension" />
        <attr name="tagText" format="reference|string" />
        <attr name="radius" format="reference|dimension" />
    </declare-styleable>
</resources>